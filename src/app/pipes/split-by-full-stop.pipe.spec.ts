import { SplitByFullStopPipe } from './split-by-full-stop.pipe';

describe('SplitByFullStopPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitByFullStopPipe();
    expect(pipe).toBeTruthy();
  });
});
